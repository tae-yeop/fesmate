# 나의 티켓북 (TicketBook) 기술 설계

> **문서 상태:** 설계 중
> **우선순위:** P2
> **마지막 업데이트:** 2025-12-23

---

## 목차

1. [개요](#개요)
2. [핵심 기능](#핵심-기능)
3. [UI/UX 설계](#uiux-설계)
4. [티켓 플립 애니메이션](#티켓-플립-애니메이션)
5. [데이터 구조](#데이터-구조)
6. [구현 계획](#구현-계획)

---

## 개요

### 목적
- 다녀온 공연의 티켓을 사진으로 보관하고 컬렉션화
- 실제 티켓북을 넘기는 듯한 인터랙티브 경험 제공
- 추억을 기록하고 공유할 수 있는 디지털 기념품

### PRD 요구사항 (6.16)
- 티켓 사진 등록
- AI 편집 (개인정보 마스킹) - P2 후반
- 티켓 컬렉션 뷰
- SNS/카카오 공유

---

## 핵심 기능

### Phase 1: 기본 기능
1. **티켓 등록**
   - 사진 촬영/갤러리 선택
   - 행사 연결 (다녀온 행사 목록에서 선택)
   - 메모 추가 (날짜, 좌석, 감상 등)

2. **티켓북 뷰어**
   - 실제 책처럼 넘기는 플립 애니메이션
   - 티켓 앞면/뒷면 지원
   - 시간순/행사별 정렬

3. **티켓 상세**
   - 확대 보기
   - 행사 정보 연결
   - 메모 편집

### Phase 2: 고도화
1. **개인정보 마스킹**
   - 좌석번호, 예매번호 블러/스티커
   - 수동 영역 지정 마스킹

2. **공유 기능**
   - 티켓 이미지 저장
   - SNS 공유 (인스타그램 스토리 최적화)
   - 카카오톡 공유

3. **통계 연동**
   - 공연로그와 연계
   - 티켓 수집 배지

---

## UI/UX 설계

### 진입점
```
MyFes 탭
├── 내 일정 (schedule)
├── 내 크루 (crew)
├── 공연로그 (gonglog)
└── 티켓북 (ticketbook) ← 신규
```

### 화면 구성

#### 1. 티켓북 메인 (책 형태)
```
┌─────────────────────────────────────┐
│  나의 티켓북           [+ 추가]     │
├─────────────────────────────────────┤
│                                     │
│    ┌─────────┐   ┌─────────┐       │
│    │ 🎫      │   │ 🎫      │       │
│    │ 티켓1   │   │ 티켓2   │       │
│    │         │   │         │       │
│    │ 2024.12 │   │ 2024.11 │       │
│    └─────────┘   └─────────┘       │
│         ↑ 터치하면 플립 →          │
│                                     │
│    [◀ 이전]    3/12    [다음 ▶]    │
│                                     │
└─────────────────────────────────────┘
```

#### 2. 플립 뷰 (전체화면)
```
┌─────────────────────────────────────┐
│ ← 닫기              [공유] [편집]  │
├─────────────────────────────────────┤
│                                     │
│         ┌───────────────┐           │
│         │               │           │
│         │   티켓 이미지  │           │
│         │               │           │
│         │               │           │
│         │   (앞면)      │           │
│         │               │           │
│         └───────────────┘           │
│              ↺ 탭하여 뒤집기        │
│                                     │
│  ◀ 스와이프하여 이전/다음 티켓 ▶   │
│                                     │
├─────────────────────────────────────┤
│  🎵 2024 IU Concert               │
│  📅 2024.12.15 (일) 18:00          │
│  📍 KSPO DOME                      │
│  💬 "최고의 공연이었다..."         │
└─────────────────────────────────────┘
```

#### 3. 티켓 등록 모달
```
┌─────────────────────────────────────┐
│  티켓 등록                    [×]   │
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐   │
│  │                             │   │
│  │   [📷 사진 촬영]            │   │
│  │   [🖼️ 갤러리에서 선택]      │   │
│  │                             │   │
│  └─────────────────────────────┘   │
│                                     │
│  연결할 행사                        │
│  ┌─────────────────────────────┐   │
│  │ 🔍 다녀온 행사 검색...       │   │
│  └─────────────────────────────┘   │
│                                     │
│  메모 (선택)                        │
│  ┌─────────────────────────────┐   │
│  │ 좌석, 동행자, 감상 등...     │   │
│  └─────────────────────────────┘   │
│                                     │
│  [ 뒷면 사진 추가 (선택) ]          │
│                                     │
│        [등록하기]                   │
└─────────────────────────────────────┘
```

---

## 티켓 플립 애니메이션

### 기술 선택지

#### Option 1: CSS 3D Transform (권장)
```css
/* 기본 플립 */
.ticket-card {
  perspective: 1000px;
}

.ticket-inner {
  transition: transform 0.6s;
  transform-style: preserve-3d;
}

.ticket-card.flipped .ticket-inner {
  transform: rotateY(180deg);
}

.ticket-front, .ticket-back {
  backface-visibility: hidden;
}

.ticket-back {
  transform: rotateY(180deg);
}
```

**장점:**
- 네이티브 CSS, 추가 라이브러리 불필요
- GPU 가속으로 부드러운 애니메이션
- 모바일 성능 우수

**단점:**
- 복잡한 페이지 넘김 효과는 구현 어려움

#### Option 2: Framer Motion (선택)
```tsx
import { motion, AnimatePresence } from "framer-motion";

const flipVariants = {
  front: { rotateY: 0 },
  back: { rotateY: 180 },
};

<motion.div
  animate={isFlipped ? "back" : "front"}
  variants={flipVariants}
  transition={{ duration: 0.6, ease: "easeInOut" }}
  style={{ transformStyle: "preserve-3d" }}
>
  {/* 티켓 내용 */}
</motion.div>
```

**장점:**
- 선언적 API, 복잡한 시퀀스 쉽게 구현
- 제스처 지원 (드래그, 스와이프)
- React와 자연스럽게 통합

**단점:**
- 번들 크기 증가 (~30KB gzipped)
- 이미 프로젝트에 없음 (설치 필요)

#### Option 3: react-pageflip (책 넘김 전문)
```tsx
import HTMLFlipBook from "react-pageflip";

<HTMLFlipBook width={300} height={400}>
  <div className="page">티켓 1</div>
  <div className="page">티켓 2</div>
</HTMLFlipBook>
```

**장점:**
- 실제 책 넘기는 효과 (페이지 굴곡 포함)
- 터치/드래그 제스처 내장

**단점:**
- 고정 크기, 반응형 어려움
- 커스터마이징 제한적
- 유지보수 불확실 (작은 라이브러리)

### 권장 구현: CSS + 커스텀 훅

```tsx
// hooks/useTicketFlip.ts
import { useState, useCallback } from "react";

export function useTicketFlip() {
  const [isFlipped, setIsFlipped] = useState(false);
  const [currentIndex, setCurrentIndex] = useState(0);

  const flip = useCallback(() => {
    setIsFlipped(prev => !prev);
  }, []);

  const next = useCallback((total: number) => {
    setIsFlipped(false);
    setTimeout(() => {
      setCurrentIndex(prev => (prev + 1) % total);
    }, 150);
  }, []);

  const prev = useCallback((total: number) => {
    setIsFlipped(false);
    setTimeout(() => {
      setCurrentIndex(prev => (prev - 1 + total) % total);
    }, 150);
  }, []);

  return { isFlipped, currentIndex, flip, next, prev, setCurrentIndex };
}
```

### 애니메이션 상세

#### 1. 앞뒤 플립 (Y축 회전)
- 티켓 탭 시 180도 회전
- 앞면 ↔ 뒷면 전환
- duration: 0.5s, ease: cubic-bezier(0.4, 0, 0.2, 1)

#### 2. 좌우 슬라이드 (페이지 전환)
- 스와이프 제스처로 다음/이전 티켓
- X축 이동 + 페이드
- duration: 0.3s

#### 3. 책 펼침 효과 (고급, Phase 2)
- 티켓북 열 때 책 펼치는 애니메이션
- 3D perspective 활용
- 선택적 구현

### 성능 최적화

1. **will-change 사용**
   ```css
   .ticket-inner {
     will-change: transform;
   }
   ```

2. **이미지 최적화**
   - 썸네일 (200px) 목록용
   - 원본 (1200px) 상세용
   - lazy loading

3. **GPU 가속**
   - transform3d(0,0,0) 트리거
   - 복잡한 그림자는 정적 이미지로

---

## 데이터 구조

### TypeScript 타입

```typescript
// src/types/ticketbook.ts

export interface Ticket {
  id: string;

  // 이미지
  frontImage: TicketImage;
  backImage?: TicketImage;       // 뒷면 (선택)

  // 연결 정보
  eventId: string;               // 연결된 행사
  eventTitle: string;            // 행사 제목 (denormalized)
  eventDate: Date;               // 행사 날짜

  // 메타데이터
  memo?: string;                 // 사용자 메모
  seat?: string;                 // 좌석 정보
  companion?: string;            // 동행자

  // 마스킹 (Phase 2)
  maskAreas?: MaskArea[];        // 마스킹 영역

  // 시스템
  createdAt: Date;
  updatedAt: Date;
}

export interface TicketImage {
  id: string;
  url: string;                   // data URL 또는 https
  thumbnailUrl: string;
  width: number;
  height: number;
}

export interface MaskArea {
  x: number;                     // % 기준
  y: number;
  width: number;
  height: number;
  type: "blur" | "sticker";
  stickerId?: string;            // 스티커 타입일 때
}

export interface TicketBook {
  tickets: Ticket[];
  sortBy: "date" | "event" | "added";
  sortOrder: "asc" | "desc";
}
```

### localStorage 구조

```json
{
  "fesmate_ticketbook": {
    "tickets": [
      {
        "id": "ticket_1703318400000",
        "frontImage": {
          "id": "img_front_xxx",
          "url": "data:image/jpeg;base64,...",
          "thumbnailUrl": "data:image/jpeg;base64,...",
          "width": 800,
          "height": 1200
        },
        "backImage": null,
        "eventId": "e1",
        "eventTitle": "2024 IU Concert",
        "eventDate": "2024-12-15T09:00:00Z",
        "memo": "생일 기념 공연! 최고였다",
        "seat": "VIP A구역 12열 5번",
        "companion": "친구 3명",
        "maskAreas": [],
        "createdAt": "2024-12-16T03:00:00Z",
        "updatedAt": "2024-12-16T03:00:00Z"
      }
    ],
    "sortBy": "date",
    "sortOrder": "desc"
  }
}
```

---

## 구현 계획

### Phase 1: 기본 구현

#### 1단계: 타입 및 Context
- [ ] `src/types/ticketbook.ts` 생성
- [ ] `src/lib/ticketbook-context.tsx` 생성
- [ ] localStorage CRUD 구현

#### 2단계: 티켓 등록
- [ ] `TicketUploadModal` 컴포넌트
- [ ] 기존 ImageUploader 활용
- [ ] 행사 검색/선택 UI

#### 3단계: 플립 애니메이션
- [ ] `TicketCard` 컴포넌트 (CSS 3D)
- [ ] `useTicketFlip` 훅
- [ ] 스와이프 제스처 (터치)

#### 4단계: MyFes 탭 통합
- [ ] 티켓북 탭 추가
- [ ] 그리드 뷰 (썸네일 목록)
- [ ] 상세 뷰 (플립 모드)

### Phase 2: 고도화

#### 5단계: 마스킹
- [ ] 영역 선택 UI
- [ ] 블러 적용
- [ ] 스티커 오버레이

#### 6단계: 공유
- [ ] 이미지 다운로드
- [ ] 인스타그램 스토리 포맷
- [ ] 카카오톡 공유

#### 7단계: 배지 연동
- [ ] "첫 티켓 등록" 배지
- [ ] "티켓 컬렉터" 배지 (10/50/100장)

---

## 파일 구조

```
src/
├── types/
│   └── ticketbook.ts              # 타입 정의
├── lib/
│   └── ticketbook-context.tsx     # Context + localStorage
├── components/
│   └── ticketbook/
│       ├── index.ts               # 배럴 export
│       ├── TicketCard.tsx         # 플립 카드 컴포넌트
│       ├── TicketGrid.tsx         # 그리드 목록
│       ├── TicketViewer.tsx       # 전체화면 뷰어
│       ├── TicketUploadModal.tsx  # 등록 모달
│       └── TicketFlip.css         # 플립 애니메이션 스타일
└── app/
    └── myfes/
        └── page.tsx               # 티켓북 탭 추가
```

---

## 참고 자료

- [CSS 3D Transforms](https://developer.mozilla.org/en-US/docs/Web/CSS/transform-function/rotateY)
- [Framer Motion](https://www.framer.com/motion/)
- [react-pageflip](https://github.com/nickmanyak/react-pageflip)
- [PRD 6.16](/docs/PRD_fesmate_v0.5_UPDATED.md)

---

## 다음 단계

1. 설계 승인 후 Phase 1 구현 시작
2. CSS 플립 애니메이션 프로토타입
3. TicketBookContext 구현
4. MyFes 탭에 통합
