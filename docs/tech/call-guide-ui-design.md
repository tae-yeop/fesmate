# 콜가이드 재생바 디자인 가이드

> 콜가이드 엔트리와 재생 진행 상태를 명확히 구분하는 UI/UX 설계

## 1. 현재 문제점 분석

### 1.1 색상 충돌 문제

현재 구현:
```
배경: bg-gray-700 (회색)
엔트리 마커: bg-purple-400/60 (반투명 보라)
재생 진행: bg-purple-500 (보라)
핸들: bg-white (흰색)
```

**문제점:**
- 엔트리 마커(purple-400)와 재생 진행(purple-500)이 **동일 색조(hue)**
- 재생이 엔트리 구간을 지나갈 때 **구분 불가**
- 반투명 엔트리가 재생바 위에서 **시각적으로 사라짐**

### 1.2 계층 구조 문제

```
z-index 현황:
- 엔트리 마커: z-5
- 재생 진행바: z-10
- 핸들: z-20
```

재생바가 엔트리 마커 위를 덮어서 진행된 구간의 엔트리가 보이지 않음.

---

## 2. 벤치마킹 분석

### 2.1 YouTube Chapters

| 요소 | 디자인 | 특징 |
|------|--------|------|
| 배경 | 투명/회색 | 전체 타임라인 |
| 챕터 구분선 | 흰색 세로선 | 명확한 경계 |
| 재생 진행 | 빨간색 | 챕터와 완전히 다른 색 |
| 호버 | 챕터 제목 툴팁 | 미리보기 제공 |

**핵심 인사이트**: 챕터 경계선(흰색) + 진행바(빨간색)으로 **색조 분리**

### 2.2 Spotify Lyrics

| 요소 | 디자인 | 특징 |
|------|--------|------|
| 진행바 | 흰색/밝은 색 | 단순한 선형 |
| 가사 하이라이트 | 텍스트 밝기 변화 | 진행바와 분리된 UI |
| 인터랙션 | 가사 터치 → 해당 위치로 이동 | 직관적 |

**핵심 인사이트**: 타임라인 마커를 진행바가 아닌 **별도 영역**에 표시

### 2.3 SoundCloud Waveform

| 요소 | 디자인 | 특징 |
|------|--------|------|
| 배경 | 파형(회색) | 시각적 흥미 |
| 재생된 부분 | 파형(주황색) | 색상 대비 명확 |
| 마커 | 세로선 + 라벨 | 클릭 가능 |

**핵심 인사이트**: 재생 전/후를 **완전히 다른 색상**으로 표현

> 참고: [SoundCloud Waveforms](https://developers.soundcloud.com/blog/waveforms-let-s-talk-about-them/), [WaveformPlayer](https://waveformplayer.com/)

---

## 3. 디자인 옵션 분석

### 옵션 A: 색조 분리 (Hue Separation)

```
배경:        bg-gray-700
엔트리:      bg-amber-400 (노란/주황 계열)
재생 진행:   bg-purple-500 (보라)
```

**장점:**
- 최소한의 변경으로 구현 가능
- 노란색과 보라색은 보색 관계로 대비 명확

**단점:**
- 색상이 다소 산만할 수 있음
- 엔트리 타입별 색상 구분 어려움

### 옵션 B: 2-레이어 분리 (Two-Layer Design)

```
[상단 레이어] 엔트리 마커 (진행바 위)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[하단 레이어] 재생 진행바
```

**장점:**
- 마커와 진행바가 물리적으로 분리
- 각각 독립적인 색상 사용 가능

**단점:**
- 세로 공간 더 필요
- 클릭 영역 복잡

### 옵션 C: 마커 + 진행바 오버레이 (Marker Overlay) ⭐ 권장

```
┌─────────────────────────────────────────────┐
│ ▼   ▼      ▼    ▼         ▼   ▼    ▼       │ ← 상단 마커 (삼각형)
├─────────────────────────────────────────────┤
│████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░│ ← 진행바
│  재생됨      미재생                          │
└─────────────────────────────────────────────┘
```

**장점:**
- 마커가 진행바와 완전히 분리
- YouTube 챕터 스타일과 유사한 친숙한 UX
- 마커에 타입별 색상/아이콘 적용 가능

**단점:**
- 구현 복잡도 증가

### 옵션 D: 엔트리 타입별 색상 (Type-based Colors)

```
떼창:    bg-blue-400
콜:      bg-orange-400
점프:    bg-green-400
조명:    bg-yellow-400
...
```

**장점:**
- 재생바에서 어떤 타입인지 즉시 파악
- 기존 타입 색상 시스템과 일관성

**단점:**
- 여러 색상이 겹치면 복잡해 보임
- 접근성(색맹) 고려 필요

### 옵션 E: 하이브리드 (Hybrid) ⭐⭐ 최종 권장

**옵션 C + D 결합:**

```
┌──────────────────────────────────────────────────┐
│ 🎵  📣     🦘   👏        📱  🎵    👋          │ ← 마커 (이모지 아이콘)
├──────────────────────────────────────────────────┤
│███████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░│ ← 재생바 (단색)
│  cyan/teal      gray                             │
└──────────────────────────────────────────────────┘
```

**구성:**
- **상단 마커**: 엔트리 타입 아이콘 (이모지) 표시
- **하단 진행바**: 단순한 단색 (teal/cyan 계열)
- **호버/활성**: 해당 엔트리 강조 + 툴팁

---

## 4. 최종 권장안: 하이브리드 마커 디자인

### 4.1 시각적 구조

```
┌─────────────────────────────────────────────────────────┐
│                    콜가이드 재생바                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   🎵      📣   🦘       👏          📱   🎵    👋      │  ← 마커 레이어
│   │       │    │        │           │    │     │       │
│   ▼       ▼    ▼        ▼           ▼    ▼     ▼       │
├───┬───────┬────┬────────┬───────────┬────┬─────┬───────┤
│███│███████│░░░░│░░░░░░░░│░░░░░░░░░░░│░░░░│░░░░░│░░░░░░░│  ← 진행바
│   │       │    │        │           │    │     │       │
│ ← 재생됨 →│← 현재 위치                                   │
└───────────────────────────────────────────────────────┘
```

### 4.2 색상 팔레트

| 요소 | 색상 | Tailwind | 용도 |
|------|------|----------|------|
| 진행바 배경 | 진회색 | `bg-gray-700` | 미재생 영역 |
| 재생된 부분 | 청록색 | `bg-teal-500` | 재생 완료 영역 |
| 핸들 | 흰색 | `bg-white` | 현재 위치 |
| 마커 배경 | 투명 | `bg-transparent` | 마커 레이어 |
| 마커 선 | 흰색/밝은회색 | `bg-white/50` | 엔트리 위치 표시 |
| 활성 마커 | 노란색 | `bg-yellow-400` | 현재 재생 중인 엔트리 |

**색상 선택 이유:**
- **Teal(청록)**: 보라색(기존)과 완전히 다른 색조, 음악 앱에서 자주 사용
- **Yellow(노란)**: 활성 상태 강조, 청록과 보색 관계
- **White**: 마커 경계선, 고대비 보장

### 4.3 마커 디자인 상세

```tsx
// 마커 유형
interface MarkerStyle {
  idle: {
    line: "bg-white/30",      // 기본 상태
    icon: "opacity-70",
  },
  active: {
    line: "bg-yellow-400",     // 현재 재생 중
    icon: "opacity-100 scale-125",
    glow: "shadow-yellow-400/50",
  },
  passed: {
    line: "bg-teal-300",       // 이미 지나간
    icon: "opacity-50",
  },
}
```

### 4.4 인터랙션

| 상태 | 시각적 피드백 |
|------|--------------|
| 기본 | 아이콘 + 얇은 세로선 |
| 호버 | 아이콘 확대 + 툴팁 (텍스트, 시간) |
| 활성 | 노란색 강조 + 펄스 애니메이션 |
| 클릭 | 해당 시간으로 이동 |

### 4.5 접근성 고려사항

> 참고: [W3C Media Accessibility](https://www.w3.org/WAI/media/av/), [WCAG Contrast Guidelines](https://www.w3.org/WAI/media/av/player/)

1. **색상만으로 정보 전달하지 않음**
   - 아이콘(이모지)으로 타입 구분
   - 호버 시 텍스트 툴팁 제공

2. **충분한 대비**
   - 진행바: teal-500 on gray-700 (4.5:1 이상)
   - 마커: white on gray-900 배경 (7:1 이상)

3. **키보드 접근성**
   - 좌/우 화살표: 10초 이동
   - Tab: 다음 마커로 이동
   - Enter: 마커 위치로 점프

---

## 5. 구현 가이드

### 5.1 컴포넌트 구조

```tsx
<div className="relative">
  {/* 마커 레이어 (상단) */}
  <div className="h-6 relative flex items-end">
    {entries.map(entry => (
      <EntryMarker
        key={entry.id}
        entry={entry}
        isActive={activeEntry?.id === entry.id}
        isPassed={currentTime > entry.endTime}
        position={(entry.startTime / duration) * 100}
        onClick={() => seekTo(entry.startTime)}
      />
    ))}
  </div>

  {/* 진행바 레이어 (하단) */}
  <div className="h-2 bg-gray-700 rounded-full relative">
    {/* 재생된 부분 */}
    <div
      className="h-full bg-teal-500 rounded-full"
      style={{ width: `${progress}%` }}
    />
    {/* 마커 위치 선 */}
    {entries.map(entry => (
      <div
        key={entry.id}
        className="absolute top-0 w-0.5 h-full bg-white/30"
        style={{ left: `${(entry.startTime / duration) * 100}%` }}
      />
    ))}
    {/* 핸들 */}
    <div className="absolute top-1/2 -translate-y-1/2 w-3 h-3 bg-white rounded-full" />
  </div>
</div>
```

### 5.2 EntryMarker 컴포넌트

```tsx
function EntryMarker({ entry, isActive, isPassed, position, onClick }) {
  const config = CALL_TYPE_CONFIG[entry.type];

  return (
    <button
      onClick={onClick}
      className={cn(
        "absolute bottom-0 flex flex-col items-center transition-all",
        "hover:scale-110 focus:outline-none focus:ring-2 focus:ring-yellow-400",
        isActive && "scale-125",
        isPassed && "opacity-50"
      )}
      style={{ left: `${position}%`, transform: "translateX(-50%)" }}
      title={`${formatTime(entry.startTime)} - ${entry.text}`}
    >
      {/* 아이콘 */}
      <span
        className={cn(
          "text-sm transition-all",
          isActive && "animate-pulse text-yellow-400"
        )}
      >
        {config.icon}
      </span>
      {/* 연결선 */}
      <div
        className={cn(
          "w-0.5 h-2",
          isActive ? "bg-yellow-400" : isPassed ? "bg-teal-300" : "bg-white/30"
        )}
      />
    </button>
  );
}
```

### 5.3 색상 변수 (CSS Variables)

```css
:root {
  /* 콜가이드 진행바 */
  --cg-bar-bg: theme('colors.gray.700');
  --cg-bar-progress: theme('colors.teal.500');
  --cg-bar-handle: theme('colors.white');

  /* 마커 */
  --cg-marker-idle: theme('colors.white / 30%');
  --cg-marker-active: theme('colors.yellow.400');
  --cg-marker-passed: theme('colors.teal.300');
}
```

---

## 6. 대안 디자인 (참고)

### 6.1 파형 스타일 (SoundCloud 영감)

엔트리 구간을 파형 높이로 표현:

```
     ▄▄▄   ▄▄▄▄▄    ▄▄    ▄▄▄▄▄▄▄   ▄▄
  ███████████████████████████████████████
```

- 장점: 시각적으로 매력적
- 단점: 실제 오디오 파형 데이터 필요, 구현 복잡

### 6.2 세그먼트 분할 스타일 (YouTube Chapters 영감)

진행바를 엔트리 구간으로 분할:

```
│ 떼창 │ 콜  │ 점프 │ 박수 │ 조명 │ 떼창 │
```

- 장점: 매우 명확한 구분
- 단점: 엔트리 없는 구간 처리 복잡

---

## 7. 구현 우선순위

### Phase 1: 즉시 적용 (색상 변경)
- [ ] 재생 진행바 색상: purple → teal
- [ ] 엔트리 마커 색상: purple → white/amber

### Phase 2: 마커 레이어 분리
- [ ] 상단 마커 레이어 추가
- [ ] 아이콘 기반 마커 구현
- [ ] 활성 마커 강조 효과

### Phase 3: 인터랙션 개선
- [ ] 호버 툴팁
- [ ] 키보드 네비게이션
- [ ] 모바일 터치 최적화

---

## 8. 참고 자료

### 디자인 영감
- [YouTube Chapters](https://codegena.com/how-to-add-timestamps-description-for-enabling-youtube-chapters/)
- [Spotify Lyrics Feature UX](https://medium.com/@marion.bonin/adding-a-lyrics-feature-to-spotify-4da2a8eab579)
- [SoundCloud Waveforms](https://developers.soundcloud.com/blog/waveforms-let-s-talk-about-them/)
- [WaveformPlayer](https://waveformplayer.com/)

### 접근성
- [W3C Media Accessibility](https://www.w3.org/WAI/media/av/)
- [WCAG Contrast Guidelines](https://www.w3.org/WAI/media/av/player/)
- [Non-text Contrast Requirements](https://www.boia.org/blog/checklist-for-creating-accessible-videos)

### 진행바 UX
- [Progress Bar Best Practices](https://www.justinmind.com/blog/10-inspiring-progress-bars-that-delight-users/)
- [Figma Spotify UI Concept](https://www.figma.com/community/file/1538075529012283079/spotify-music-player-ui-interactive-playback-lyrics-view)
